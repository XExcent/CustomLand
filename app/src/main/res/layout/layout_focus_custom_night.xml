<?xml version="1.0" encoding="utf-8"?><!-- 自定义焦点通知布局 - 暗色模式 (也用于超级岛展开) -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:padding="16dp">

    <!-- 左侧图标 -->
    <ImageView
        android:id="@+id/ivIcon"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginEnd="16dp"
        android:contentDescription="图标"
        android:scaleType="fitCenter"
        tools:src="@drawable/ic_receipt" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- 上半部分：图标 + 描述 + 主要内容 + 按钮 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- 右侧文本区域 -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <!-- 小号描述文本 -->
                <TextView
                    android:id="@+id/tvLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#99FFFFFF"
                    android:textSize="14sp"
                    tools:text="取餐码" />

                <!-- 大号主要内容 -->
                <TextView
                    android:id="@+id/tvMainContent"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textColor="#DEFFFFFF"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    tools:text="12345" />

            </LinearLayout>

            <!-- 右侧按钮 -->
            <TextView
                android:id="@+id/btnAction"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:background="@drawable/focus_button_dark"
                android:paddingHorizontal="16dp"
                android:paddingVertical="8dp"
                android:textColor="#98CCF9"
                android:textSize="14sp"
                android:textStyle="bold"
                tools:text="已取" />

        </LinearLayout>

        <!-- 左侧描述文字 -->
        <TextView
            android:id="@+id/tvDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#99FFFFFF"
            android:textSize="14sp"
            tools:text="雪王大圣代\n蜜雪冰城" />

    </LinearLayout>

</LinearLayout>
